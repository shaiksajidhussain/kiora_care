# Resend API Key - REQUIRED for sending emails
# Get your API key from https://resend.com/api-keys
# Or use existing key from AWS ECS environment: re_ER61dsiM_53SzCuWrGJFPvmYEeaKDK6aw
RESEND_API_KEY=re_ER61dsiM_53SzCuWrGJFPvmYEeaKDK6aw

# FROM Email Address - The email address that appears as sender
# IMPORTANT: Resend requires verified domain. Options:
# 1. Use Resend's default: onboarding@resend.dev (works immediately)
# 2. Use your Gmail if verified in Resend dashboard
# 3. Verify kiora.care domain at https://resend.com/domains (then use care@kiora.care)
FROM_EMAIL=kiora@kiora.care

# Target email address where form submissions will be sent
# Defaults to sanjusazid0@gmail.com if not set
TARGET_EMAIL=kiora@kiora.care

# Server port (defaults to 3001, or 8000 for AWS ECS)
PORT=3001

# Database Configuration (Optional - for storing submissions)
# If not set, emails will still be sent but submissions won't be saved to database
# Format: postgresql://user:password@host:5432/database?schema=public
DATABASE_URL=postgresql://postgresAdmin:Bhavani@987!@db.bhavanichits.com/database-1?schema=public

# Allowed CORS origins (comma-separated)
# Use '*' to allow all origins, or specify domains like: http://localhost:8080,https://kiora.care
# Defaults to '*' if not set
ALLOWED_ORIGIN=*
