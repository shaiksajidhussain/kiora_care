# Environment Configuration
# Copy this file to .env and update the values

# Environment Mode: Set to "production" or "development"
# - production or prod → Uses https://kiora-care-backend.vercel.app
# - development or dev → Uses http://localhost:3001
# - Leave unset → Defaults to localhost (development)
VITE_ENV=development

# Optional: Override backend URL directly (takes priority over VITE_ENV)
# VITE_BACKEND_URL=https://kiora-care-backend.vercel.app
